name: stl
description: A high-performance C++ STL inspired library for Dart & Flutter. Unlock advanced data structures, functional wrappers, and C++23 ranges.
version: 0.3.4
repository: https://github.com/jedizlapulga/stl

environment:
  sdk: ^3.11.3

# Add regular dependencies here.
dependencies:
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

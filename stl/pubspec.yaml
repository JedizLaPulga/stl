name: stl
description: A performance-oriented implementation of STL patterns (Vector, List, algorithms) for Dart.
version: 0.1.1
repository: https://github.com/jedizlapulga/stl

environment:
  sdk: ^3.11.3

# Add regular dependencies here.
dependencies:
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
